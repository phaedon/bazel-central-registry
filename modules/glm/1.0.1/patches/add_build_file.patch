--- BUILD.bazel
+++ BUILD.bazel
@@ -0,0 +1,11 @@
+cc_library(
+    name = "glm",
+    hdrs = glob([
+        "**/*.h",
+        "**/*.hpp",
+    ]),
+    includes = ["."],
+    textual_hdrs = glob(["**/*.inl"]),
+    visibility = ["//visibility:public"],
+    include_prefix = "glm",
+)
